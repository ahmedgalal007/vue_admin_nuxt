// vue-oidc-client-nuxt-auth
// vueOidcClientNuxtAuth.js
export default async function (context) {
  // const redirectUrl = await context.axios.getAsync(context.route.path);
  // if (!context.store.state.authenticated) {
  //   return context.redirect('/login')
  // }
  // console.log('CONTEXT:', context.app.store.$router)
  // // ///////////////////////////////////////////////////////////
  // if (context.ssrContext) return
  // console.log('context_1122333:', context)
  // if (context.app.auth === false) return
  // const user = await context.$auth.getUser()
  // if (!user || user.expired) {
  //   await context.$auth.signinRedirect()
  // }
  // console.log('User_1122333:', user)
  // if (!context.store?.$auth?.$state?.loggedIn) {
  //   // write something to do if he is not logged in
  //   console.log('write something to do if he is not logged in')
  // }
  // ///////////////////////////////////////////////////////////
}
